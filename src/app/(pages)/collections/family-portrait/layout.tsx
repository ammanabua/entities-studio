export const metadata = {
  title: 'Family Portrait - Entities Studio',
  description: '',
}

export default function FamilyPortraitLayout({ children }: { children: React.ReactNode }) {
  return <div className="font-krona ">{children}</div>
}
